package com.sandbox.chat.core.placeOrder;

public interface PlaceOrderContract {
    interface View{
        // fragment functions
    }

    interface Presenter {
        // functions from the activity (functionality)
    }

    interface Interactor {
        // firebase functions
    }

    interface  OnPlaceOrderListener{
        // functions from presenter -- what next?
    }
}
